#-------------------------------------------------------------------------------
# My hyprland config
# https://github.com/unclassedpenguin/dotfiles.git
# https://wiki.hyprland.org/Configuring/
#-------------------------------------------------------------------------------


#-------------------------------------------------------------------------------
# Monitors
#-------------------------------------------------------------------------------

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,preferred,auto,1.25
monitor=HDMI-A-2,1920x1080@60,0x0,1,mirror,eDP-1


#-------------------------------------------------------------------------------
# Programs
#-------------------------------------------------------------------------------

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = nemo
$menu = rofi -show drun -font 'mono 11'


#-------------------------------------------------------------------------------
# Autostart
#-------------------------------------------------------------------------------

# hyprpaper is the program that sets the background. randowallpaper (sic) is a script 
# that is in ~/.bin that picks a random wallpaper from ~/Pictures/backgrounds
exec-once = eww daemon & eww open bar & hyprpaper & sleep 1 && randowallpaper


#-------------------------------------------------------------------------------
# Environment Variables
#-------------------------------------------------------------------------------

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = HYPRCURSOR_THEME,Adwaita
env = HYPRCURSOR_SIZE,22

env = XCURSOR_THEME,Adwaita
env = XCURSOR_SIZE,22


#-------------------------------------------------------------------------------
# Permissions
#-------------------------------------------------------------------------------

# See https://wiki.hyprland.org/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#-------------------------------------------------------------------------------
# Look and Feel
#-------------------------------------------------------------------------------

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 5
    gaps_out = 5

    border_size = 1

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    # Blue (default)
    #col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    # Red
    #col.active_border = rgba(245,39,145,0.56) rgba(124,43,138,0.87) 45deg
    # Green
    #col.active_border = rgba(3a5683ff) rgba(51cb20ff) 90deg
    # Grey
    #col.active_border = rgba(6d887fff) rgba(3d5251ff) 90deg
    # Purple
    col.active_border = rgba(704ec0ff) rgba(3d5251ff) 55deg
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    # Layout
    layout = dwindle
    #layout = master
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 5
    #rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity =0.75 

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1
        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more
    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    force_split = 2
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}


#-------------------------------------------------------------------------------
# Input
#-------------------------------------------------------------------------------

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options = ctrl:swapcaps
    kb_rules =

    # 1 Focus follows all mouse movements
    # 2 Focus will change on a mouse click
    #follow_mouse = 1
    follow_mouse = 2

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
    scroll_factor = 3.0

    touchpad {
        natural_scroll = false
        scroll_factor = 4.0
    }
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


#-------------------------------------------------------------------------------
# Gestures
#-------------------------------------------------------------------------------

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    workspace_swipe = on
    workspace_swipe_distance = 200
    workspace_swipe_create_new = false
    workspace_swipe_forever = true
}

#-------------------------------------------------------------------------------
# Keybindings
#-------------------------------------------------------------------------------

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = ALT

# Screenshot/Color Picker (requires grim and slurp)
# Screenshot entire screen & notify where it saved
bind = , Print, exec, grim "/home/tyler/Pictures/screenshots/$(date +%Y%m%d-%H%M%S)_grim".png && hyprctl notify 1 10000 0 "fontsize:20 :) Screenshot saved to ~/Pictures/screenshots/"
# Use a selection tool to get just part of the screen & notify where it saved
bind = $mainMod, Print, exec, grim -g "$(slurp)" "/home/tyler/Pictures/screenshots/$(date +%Y%m%d-%H%M%S)_grim_slurp".png && hyprctl notify 1 10000 0 "fontsize:20 :) Screenshot saved to ~/Pictures/screenshots/"
# Color picker
bind = SHIFT, Print, exec, hyprctl notify 1 20000 0 "fontsize:20 $(grim -g "$(slurp -p -b 00000000)" -t ppm - | magick - -format '%[pixel:p{0,0}]' txt:- | awk 'NR==2')"

# Key binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, B, exec, randowallpaper
bind = $mainMod, D, exec, $menu
bind = $mainMod, F, fullscreen,
bind = $mainMod, M, resizeactive, exact 65% 35%
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, X, exec, inczoom
bind = $mainMod, Z, exec, deczoom
bind = $mainMod, V, exec, hyprctl keyword cursor:zoom_factor 5
bind = $mainMod, C, exec, hyprctl keyword cursor:zoom_factor 0
bind = $mainMod, semicolon, togglesplit, # dwindle
bind = $mainMod SHIFT, SPACE, togglefloating,
bind = $mainMod SHIFT, Q, killactive,
bind = $mainMod SHIFT, E, exit,

# Launch programs
bind = $mainMod, RETURN, exec, $terminal
bind = SUPER, B, exec, brave-browser-stable
bind = SUPER, F, exec, $fileManager
bind = SUPER, G, exec, gimp
bind = SUPER, S, exec, steam
bind = SUPER, T, exec, featherpad

# Move focus with mainMod + vim keys
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, J, movefocus, d
bind = $mainMod, K, movefocus, u

# Move windows
bind = SUPER, H, swapwindow, l
bind = SUPER, L, swapwindow, r
bind = SUPER, J, swapwindow, d
bind = SUPER, K, swapwindow, u
bind = SUPER, C, exec, hyprctl dispatch centerwindow

# Resize windows
bind = $mainMod SHIFT, H, resizeactive, -10 0
bind = $mainMod SHIFT, L, resizeactive, 10 0
bind = $mainMod SHIFT, J, resizeactive, 0 10
bind = $mainMod SHIFT, K, resizeactive, 0 -10

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Special workspace (scratchpad)
bind = $mainMod, A, togglespecialworkspace, magic
bind = $mainMod SHIFT, A, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle

bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Might need these commands, instead
#bindel = ,XF86AudioRaiseVolume, exec, amixer set Master 5%+
#bindel = ,XF86AudioLowerVolume, exec, amixer set Master 5%-
#bindel = ,XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +5%
#bindel = ,XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -5%
#bindel = ,XF86AudioMute, exec, amixer set Master toggle
#bindel = ,XF86AudioMute, exec, pactl set-sink-mute 0 toggle

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous


#-------------------------------------------------------------------------------
# Windows and Workspaces
#-------------------------------------------------------------------------------

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

# Launch kitty on empty magic workspace 
workspace = special:magic, on-created-empty:[float] kitty
windowrule = float,class:^(kitty)$,title:^(kitty)$, onworkspace:magic
windowrulev2 = size 1100 675, class:kitty

# Don't let mpv go transparent
windowrule = opacity 1.0 override 1.0 override 1.0, class:mpv

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]
